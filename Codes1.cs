using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Codes1 : MonoBehaviour
{
    
    public void changeView(string a)
    {
        Application.LoadLevel (a) ;
    }
    // Start is called before the first frame update
 
}
